import 'package:flutter/material.dart';

class Mantarini extends StatelessWidget {
  const Mantarini({Key? key}) : super(key: key);

  static const routeName = 'mantarini';

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('MANTARINI'),
      ),
    );
  }
}
